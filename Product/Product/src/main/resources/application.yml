spring:

  application:
    name: Product

  datasource:
    url: jdbc:mysql://localhost:3306/productdb
    username: root
    password: Adivi@8123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 9090